# INTERACTION-PATTERN.md — 选项式交互规范

> 灵感来自 Claude Code：需求不明确时，主动给选项，不瞎猜，不反问到死。

## 适用场景

主要用在**本地生活管家项目**的开发过程中。当你的需求不够具体、或者有多种实现路径时，我会主动列出选项让你选。

## 触发条件

**给选项的情况：**

1. **范围模糊** — "帮我优化一下" → 优化性能？可读性？功能？
2. **多条实现路径** — 比如数据来源可以用 API/爬虫/模拟数据，各有取舍
3. **缺少关键参数** — 哪个城市？什么菜系？预算多少？什么时间？
4. **架构/设计决策** — 比如配置放哪里、模块怎么拆分、错误处理策略
5. **优先级不确定** — 待做清单里有好几件事，先做哪个？

**直接做的情况：**
- 需求明确，没有歧义
- 只有一个合理方案
- 你说"随便"或"你决定"
- 是之前讨论过的、有明确结论的事

## 选项格式

### 基础版（最常用）

```
这个有几种做法：

① [方案名] — [一句话说明]
   代价：[时间/复杂度/风险]

② [方案名] — [一句话说明]
   代价：[时间/复杂度/风险]

③ [方案名] — [一句话说明]
   代价：[时间/复杂度/风险]

选一个？或者告诉我更多细节。
```

> 只有确实有明显最优解时，才在末尾加一句：`→ 推荐②，因为[理由]`。否则不加。

### 精简版（快速决策）

```
A还是B？
A: [说明]
B: [说明]
```

### 详细版（重大决策）

```
## 现状
[当前是什么样]

## 方案对比

| | 方案A | 方案B | 方案C |
|---|---|---|---|
| 做法 | ... | ... | ... |
| 改动量 | ... | ... | ... |
| 风险 | ... | ... | ... |
| 后续维护 | ... | ... | ... |

你怎么看？
```

### 待办优先级版

```
目前待做清单：
1. [任务A] — 影响：[高/中/低]，耗时：[估算]
2. [任务B] — 影响：[高/中/低]，耗时：[估算]
3. [任务C] — 影响：[高/中/低]，耗时：[估算]

先做哪个？还是你有新的想法？
```

## 设计原则

1. **最多 5 个选项** — 少而精，2-3 个也行，别超过 5 个
2. **每个选项有明确差异** — 不要两个几乎一样的
3. **标注代价** — 时间、复杂度、风险、后续维护
4. **推荐不强求** — 只有确实有明显最优解时才给推荐，否则保持中立让用户选
5. **留出口** — "或者你有别的想法？" 不强迫选
6. **先干活再问** — 能先看代码/现状再给选项，而不是空对空

## 项目里的典型场景

### 场景1：功能方向模糊

**你说：** "帮我加上天气功能"

**我的回复：**
```
天气功能有几种做法：

① **高德天气 API** — 直接用现有 API key，查实时天气
   代价：最简单，半小时搞定

② **wttr.in** — 免费天气服务，支持中文
   代价：不需要额外 key，但功能简单

③ **天气 + 出行建议联动** — 下雨天自动推荐室内餐厅
   代价：需要串两个模块，1-2小时

→ 推荐先做①，后面再加③的联动。可以吗？
```

### 场景2：技术方案取舍

**你说：** "搜索结果太慢了"

**我的回复：**
```
优化搜索有几种思路：

① **加缓存** — 同样的查询不重复调 API
   代价：简单，但只解决重复查询

② **异步并发** — 多个 API 同时请求
   代价：改动中等，效果明显

③ **减少 API 调用** — 先本地过滤再调远程
   代价：需要改逻辑，但最根本

→ 推荐②，改完立竿见影。你觉得？
```

### 场景3：项目优先级

**你说：** "继续推进吧"

**我的回复：**
```
待做清单：

1. 行程编排支持更多场景 — 影响高，1小时
2. 娱乐类型扩展 — 影响中，30分钟
3. 评价标签细化 — 影响低，30分钟

先做哪个？或者你有别的想法？
```

---

_这个文件本身也可以改——你觉得交互方式不舒服了，随时说。_

_Created: 2026-05-23 | Updated: 针对本地生活管家项目定制_
