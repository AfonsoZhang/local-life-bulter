# Python
__pycache__/
*.pyc

# 🔴 密钥 / 个人配置 —— 绝不提交（用对应的 *.example.json 代替）
config/amap_config.json
agent/models.json

# 运行时 / 个人数据 —— 含真实偏好、历史、日程，用 *.example.json 演示
config/history.json
config/session_state.json
config/preferences.json
config/calendar.json

# OpenClaw 运行时
.openclaw/
*.log
backups/

# 旧污染 git 备份，绝不提交
.git.contaminated.bak/
*.contaminated.bak/

# Claude Code 开发笔记，与提交无关
CLAUDE.md

# 空白人设模板，与管家提交无关（未被任何文档引用）
IDENTITY.md

# 本地专属：MCP 服务与演示脚本（有意只留本地，不进提交）
mcp_server.py
DEMO.md

# 运行时业务数据 —— 含真实联系方式/PII，绝不提交（仓库里保留干净 demo 版）
skills/booking/data/bookings.json
skills/review/data/reviews.json
