# AGENTS.md - 本地生活管家

## 项目简介

基于 OpenClaw 的对话式本地生活管家，通过自然语言替代传统 App UI，覆盖餐厅推荐、活动发现、日程管理、出行规划、预订支付全流程。

## ⚠️ 运行时约束的实际位置

> **注意：** OpenClaw 只从 workspace **根目录**加载 `AGENTS.md` / `SOUL.md`，不会递归加载本子目录的 `AGENTS.md`。因此本项目的运行时行为约束**不写在这里**（写在这里不会生效），而是分布在会被加载的位置：

- **身份/防幻觉/沟通**（选项式交互、默认地址、日期/数据/日程防幻觉）→ `workspace/SOUL.md`
- **平台格式/工作流**（微信格式、Emoji 约束、推荐后衔接预订）→ `workspace/AGENTS.md`（Platform Formatting 区）
- **各技能专属约束**（日程增删改、预订流程、评价收集、行程编排等）→ 各自 `skills/*/SKILL.md`（技能触发时加载）

本文件仅作项目说明文档，不承载运行时约束。
