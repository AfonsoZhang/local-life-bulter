# 🏠 全天候智能管家 — 设计文档

> 基于 OpenClaw 框架的本地生活智能助手，通过自然语言对话完成日程管理、天气播报、路径规划、美食推荐、娱乐发现等全场景服务。

## 架构总览

```
┌─────────────────────────────────────────────┐
│            IM 层（微信 / Telegram）           │
├─────────────────────────────────────────────┤
│       Agent 层（SOUL / AGENTS / TOOLS）       │
│   行为约束 · 防幻觉硬约束 · 交互范式 · 工具目录 │
├─────────────────────────────────────────────┤
│            技能层（7 个 Skills）              │
│  food-finder · travel-planner · entertainment │
│  booking · review · calendar · scheduler      │
├─────────────────────────────────────────────┤
│            核心层（Core）                     │
│  amap_api · memory · scheduler · date_resolver │
│  location_cli · cal_manager · weather · formatter │
├─────────────────────────────────────────────┤
│            数据层（Config）                   │
│   calendar.json · preferences.json · history.json │
└─────────────────────────────────────────────┘
```

**设计原则：** 分层解耦，每层职责单一。Agent 层定义"我是谁"，技能层定义"我会什么"，核心层定义"怎么做"，数据层定义"我记住了什么"。

## 核心模块

### 1. 高德地图 API 模块（`core/amap_api.py`）

统一的地理位置服务模块，为所有技能提供底层能力：

| 能力 | 说明 |
|------|------|
| POI 搜索 | 关键词搜索 + 周边搜索 |
| 地理编码 | 地址 ↔ 坐标互转 |
| 路径规划 | 驾车 / 公交 / 步行 / 骑行 |
| 天气查询 | 实况 + 预报 |
| IP 定位 | 自动获取用户所在城市 |

**设计亮点：** 所有 API 调用均有 fallback 机制，API 不可用时自动降级到模拟数据，保证服务不中断。

### 2. 日程管理模块（`core/cal_manager.py`）

自建日历系统，不依赖外部日历服务：

- **自然语言输入** → 自动解析时间、标题、地点
- **iCal 导入** → 支持 .ics 文件导入外部日程
- **冲突检测** → 推荐活动时自动检查时间冲突
- **空闲时段查询** → 找出可用时间窗口
- **路由集成** → "帮我记"、"提醒我"自动识别为日历操作

### 3. 偏好学习模块（`core/memory.py`）

从用户交互中持续学习，越用越懂你：

- **负向反馈衰减** — 用户说"不想吃" → 自动压低偏好权重
- **时间衰减** — 半衰期 30 天，越久远的偏好权重越低
- **场景化偏好** — 不同同行人有独立偏好记录（solo / couple / family / friends）
- **偏好摘要** — 综合展示正向偏好 + 负向反馈 + 场景化倾向

### 4. 播报系统（`skills/calendar/scripts/broadcast.py`）

全自动定时播报，格式固定，模型只需原样发送：

- **早安播报** — 天气 + 今日日程 + 每日打卡提醒
- **晚安播报** — 明日天气 + 当日日程（凌晨触发时正确对应当天）
- **天气预警** — 异常天气时自动触发

### 5. 智能排期器（`core/scheduler.py`）

给定候选活动 + 餐厅，生成最优时间线：

- 并行计算活动间出行时间矩阵
- 贪心算法优化活动顺序（最小化总出行时间）
- 自动在合适时段插入午餐 / 晚餐
- 计算总预算（活动费用 + 用餐估算）

## 技术亮点

### 并行调用

使用 `ThreadPoolExecutor` 实现 API 并行调用，显著降低响应延迟：

```python
# 天气 + 日历并行获取（3x 加速）
# 午餐 + 路线并行规划（3x 加速）
# 活动 + 餐厅并行搜索（3x 加速）
```

### 结构化输出校验

所有工具返回值经过 JSON Schema 校验，保证数据质量：

- 递归校验（类型 / required / enum / 范围）
- 自动补全缺失字段（数组 → `[]`，nullable 对象 → `None`）
- 支持从 JSON 代码块提取内容

### 数据呈现约束（防幻觉机制）

**问题：** 模型在转述事实性数据（日期、星期、数字）时容易产生幻觉。

**方案：** 在 SOUL.md 中定义硬约束 —— 工具输出的事实性数据必须沿用原文，禁止自行推算或转述。脚本说"周六"，回复就必须写"周六"，不能改成"周五"。

**效果：** 从 prompt 层面彻底消除数据转述错误，同时保留模型在建议、语气、格式美化上的自由度。

### 定时任务编排（全天候主动服务）

通过 OpenClaw Cron 实现全自动定时推送，是"全天候"的核心。注册配置见 [`cron/jobs.example.json`](cron/jobs.example.json)（脱敏示例，投递目标用占位符）；每个任务通过 `exec` 调用 `broadcast.py` / `calendar_cli.py`，**脚本有输出才发消息，无输出静默**——只在该说话时说话。

| 任务 | 时间 (cron) | 内容 | 触发模式 |
|------|------------|------|---------|
| 早安播报 | `0 9 * * *` | 天气 + 今日日程 + 今日待办 | 一定发 |
| 晚安播报 | `0 0 * * *` | 明日天气 + 当日日程 | 一定发 |
| 日程提醒 | `0 9-23 * * *` | 即将到来的日程 | 有则发 |
| 天气突变预警 | 每 2 小时 | 异常天气时触发 | 有则发 |
| 预订到店提醒 | `0 10-22 * * *` | 2 小时内有预订时提醒 | 有则发 |
| 评价邀请提醒 | `0 20 * * *` | 有已完成未评价的体验时询问 | 有则发 |
| 天气-日程冲突检测 | `0 8,11,14,17 * * *` | 恶劣天气 + 户外日程冲突时建议改方案 | 有则发 |

> 实现见 `skills/calendar/scripts/broadcast.py`（morning / evening / alert / booking / review / weather-check 六种模式）。

### Dashboard 可视化

Flask Dashboard 展示工具编排逻辑，每个场景可视化展示管家如何串联多个技能完成复杂任务。

## 技能联动

三个生活技能通过**区域（zone）** 关联，实现智能串联：

```
"看完电影吃什么"    → entertainment + food-finder（同区域推荐）
"周末带孩子出去玩"  → entertainment + food-finder（时间线 + 预算）
"去西湖有什么好玩的" → travel-planner + entertainment + food-finder
"约会安排"         → entertainment + food-finder（情侣场景偏好）
```

意图路由器从自然语言中识别涉及哪些技能、串联顺序、场景上下文（同行人、时段、预算），联动引擎按区域就近匹配并生成完整方案。

## 对话式交易闭环

### 核心理念：对话替代 UI

传统 App 需要 8 步（搜索→筛选→详情→预订→表单→确认→支付→评价），对话式只需 2-3 步。

### 6. 预订管理模块（`skills/booking/`）

对话式预订，替代传统 App 的表单填写：

- **参数提取** — 从自然语言中识别场所、时间、人数
- **逐项补问** — 缺少信息时一次问一个，不堆砌问题
- **模拟支付** — 预订后生成支付确认，展示完整交易闭环
- **日程联动** — 预订成功自动创建日程条目
- **到店提醒** — 预订时间前 2 小时自动提醒

### 7. 评价系统（`skills/review/`）

对话式评价，替代传统 App 的"打星 + 写评论"：

- **情感分析** — 从"不错就是有点咸"中提取正面/负面/属性评价
- **偏好更新** — 评价自动融入 memory.py 偏好学习系统
- **主动询问** — 预订完成后自动询问体验，不需要用户主动找评价入口
- **属性提取** — 区分口味、服务、环境、价格等维度

### 完整用户旅程

```
"想吃日料"              → food-finder 推荐 3 家
"第二个吧"              → 记录选择
"帮我订明天晚上7点"      → booking 创建预订 + 自动创建日程
"付了"                  → 模拟支付确认
（次日 17:00 自动提醒）   → "还有2小时到店"
（次日 21:00 自动询问）   → "那家日料怎么样？"
"环境不错，就是有点贵"    → review 记录 → 偏好更新（下次推荐注意价格）
```

**传统 App 同样的流程需要跨 3 个 App（美团搜索 + 大众点评预订 + 高德导航），7 个页面，15+ 次点击。**

### 情境感知主动服务

| 触发条件 | 行为 |
|---------|------|
| 恶劣天气 + 有户外日程 | 主动建议改到室内，推荐替代方案 |
| 预订时间前 2 小时 | 到店提醒 + 导航入口 |
| 预订完成后次日 | 主动询问体验 |
| 早安播报 + 今日有预订 | 附带今日预订提醒 |

## 项目结构

```
local-life-butler/
├── DESIGN.md              # 本文件 — 项目设计文档
├── README.md              # 项目说明 + 快速开始
├── core/
│   ├── amap_api.py        # 高德地图 API 统一封装
│   ├── cal_manager.py     # 日程管理（自建日历 + iCal）
│   ├── memory.py          # 偏好学习 + 交互历史
│   ├── formatter.py       # 结构化消息格式化（卡片式输出）
│   ├── weather.py         # 天气获取
│   ├── time_utils.py      # 时间感知工具
│   ├── chains.py          # 技能联动引擎
│   ├── router.py          # 意图路由器
│   ├── scheduler.py       # 智能排期器
│   └── tools.py           # 参数化工具注册
├── skills/
│   ├── calendar/          # 日历技能（播报 + 提醒）
│   ├── food-finder/       # 美食推荐
│   ├── travel-planner/    # 出行规划
│   ├── entertainment/     # 休闲娱乐
│   ├── booking/           # 对话式预订（预订 + 支付 + 日程联动）
│   └── review/            # 对话式评价（情感分析 + 偏好更新）
├── dashboard/
│   └── app.py             # Flask 可视化面板
└── config/
    ├── amap_config.json   # 高德 API Key
    ├── calendar.json      # 日程数据
    ├── preferences.json   # 用户偏好
    └── history.json       # 交互历史
```

## 技术栈

- **框架：** OpenClaw（Agent 运行时 + Cron 调度 + IM 路由）
- **语言：** Python 3
- **API：** 高德地图 Web 服务 API
- **通信：** IM 对话（微信）
- **数据：** JSON 本地存储
- **Dashboard：** Flask

---

> 🔊 Echo — 你说，我听，我回应。
